import 'package:flutter/material.dart';

class LongOutScreen extends StatelessWidget {
  const LongOutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}
